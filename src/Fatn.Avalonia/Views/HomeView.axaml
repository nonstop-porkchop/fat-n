<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:viewModels="clr-namespace:Fatn.Avalonia.ViewModels"
             mc:Ignorable="d" d:DesignWidth="800" d:DesignHeight="450"
             x:Class="Fatn.Avalonia.Views.HomeView">
    
    <Design.DataContext>
        <viewModels:HomeViewModel />
    </Design.DataContext>
    
    <TabControl Items="{Binding Path=TabItems}" TabStripPlacement="Left">
        <TabControl.ItemTemplate>
            <DataTemplate>
                <ContentPresenter Height="24" Width="24" Content="{Binding}">
                    <ContentPresenter.DataTemplates>
                        <DataTemplate DataType="{x:Type viewModels:InboxViewModel}">
                            <TextBlock Text="📧" />
                        </DataTemplate>
                    </ContentPresenter.DataTemplates>
                </ContentPresenter>
            </DataTemplate>
        </TabControl.ItemTemplate>
    </TabControl>
    
</UserControl>